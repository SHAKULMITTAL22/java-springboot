# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointment/{appointmentId}_delete for http method type DELETE in artillery framework
# RoostTestHash=8ae4a840f9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "appointment_appointmentId_delete_success.csv"
    fields:
      - "appointmentId"
      - "appointmentReason"
      - "appointmentComments"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Delete appointment scenario"
    flow:
      - log: "Starting DELETE request for deleting an appointment"
      - delete:
          url: "/appointment/{{ appointmentId }}?appointmentReason={{ appointmentReason }}&appointmentComments={{ appointmentComments }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "type"
            - hasProperty: "message"
            - hasProperty: "code"
            - matchesRegexp: "type=S|T|E"
            - matchesRegexp: "code=\\d{3}"
            - matchesRegreg: "message=.*"
      - think: 1
      - log: "Starting GET request to verify the deletion of the appointment"
      - get:
          url: "/appointment/{{ appointmentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
