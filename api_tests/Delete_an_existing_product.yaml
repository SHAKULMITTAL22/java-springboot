// Test generated by RoostGPT for test integrationtest-0408 using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

+ scenario: Successful API request
+   Given there is an existing product with ID 2
+   When the client sends a DELETE request to /products/2
+   Then the response status code should be 200
+   And the product with ID 2 should no longer exist

+ scenario: API request with product not found
+   Given there is no product with ID 3
+   When the client sends a DELETE request to /products/3
+   Then the response status code should be 404
+   And the response message should be "Product not found"

+ scenario: API request with invalid product id
+   Given there is no product with ID "invalid"
+   When the client sends a DELETE request to /products/"invalid"
+   Then the response status code should be 400
+   And the response message should be "Invalid product id"

+ scenario: API request with edge case of large product id
+   Given there is no product with ID 99999999999999999
+   When the client sends a DELETE request to /products/99999999999999999
+   Then the response status code should be 404
+   And the response message should be "Product not found"
