# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=cff6ff5663
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointmentConfirmation_post_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Appointment Confirmation Test Scenario"
    flow:
      - log: "Starting Appointment Confirmation Test Scenario"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "{{ customer.subscriberId }}"
            appointment:
              id: "{{ appointment.id }}"
              confirmation: "{{ appointment.confirmation }}"
      - expect:
          - statusCode: 200
          - contentType: "application/json"
          - hasProperty: "control"
          - hasProperty: "control.type"
          - hasProperty: "control.message"
          - hasProperty: "control.code"
          - headerEquals:
              Content-Type: "application/json; charset=utf-8"
          - equals:
              control.type: "S"
          - equals:
              control.code: "200"
          - equals:
              control.message: "OK"
      - think: 2
      - log: "Appointment Confirmation Request Completed"
