# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=cff6ff5663
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "appointmentConfirmation_post_success.csv"
    fields:
      - "subscriberId"
      - "appointmentId"
      - "confirmation"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Appointment Confirmation Success Flow"
    flow:
      - log: "Starting POST request for appointment confirmation"
      - post:
          url: "/appointmentConfirmation"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            appointment:
              id: "{{ appointmentId }}"
              confirmation: "{{ confirmation }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - equals: 
                path: "control.type"
                value: "S"
            - matchesRegexp:
                path: "control.message"
                regexp: "OK"
      - think: 1
