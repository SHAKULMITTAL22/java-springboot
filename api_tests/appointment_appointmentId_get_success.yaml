# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointment/{appointmentId}_get for http method type GET in artillery framework
# RoostTestHash=b69bb60b36
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "appointment_appointmentId_get_success.csv"
    skipHeader: true
    fields:
      - "appointmentId"
    delimiter: "^|^"
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      Content-Type: "application/json"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful responses for appointment retrieval"
    flow:
      - log: "Starting GET request for appointment retrieval"
      - get:
          url: "/appointment/{{ appointmentId }}"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "appointments"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - hasProperty: "appointments.lifeCycleStatus"
            - contentType: "application/json"
          headers:
            Accept: "application/json"
      - think: 1

      - log: "Starting POST request to update appointment status"
      - post:
          url: "/appointment/{{ appointmentId }}/status"
          json:
            lifeCycleStatus: "Atribuído"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - contentType: "application/json"
          headers:
            Accept: "application/json"
      - think: 1

      - log: "Starting DELETE request to cancel appointment"
      - delete:
          url: "/appointment/{{ appointmentId }}"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - contentType: "application/json"
          headers:
            Accept: "application/json"
      - think: 1

      - log: "Starting PATCH request to modify appointment details"
      - patch:
          url: "/appointment/{{ appointmentId }}"
          json:
            occurrences:
              - reason: "Updated Reason"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - contentType: "application/json"
          headers:
            Accept: "application/json"
      - think: 1
