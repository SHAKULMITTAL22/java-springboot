# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_get for http method type GET in artillery framework
# RoostTestHash=b69bb60b36
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_get_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Retrieve Appointment Scenario"
    flow:
      - log: "Starting Retrieve Appointment Scenario"
      - get:
          url: "/appointment/{{ appointmentId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasHeader:
            - "content-type"
      - expect:
          hasProperty:
            - "control"
            - "appointments"
            - "appointments.id"
            - "appointments.lifeCycleStatus"
            - "appointments.occurrences"
            - "appointments.occurrences[0].creationDateTime"
            - "appointments.occurrences[0].startDate"
            - "appointments.occurrences[0].finishDate"
            - "appointments.occurrences[0].reason"
            - "appointments.occurrences[0].reasonDetail"
      - think: 2
