# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /searchTimeSlot_get for http method type GET in artillery framework
# RoostTestHash=0e2f009f7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "searchTimeSlot_get_others.csv"
    fields:
      - "addressId"
      - "subscriberId"
      - "associatedDocument"
      - "startDate"
      - "finishDate"
      - "orderType"
      - "addressChangeFlag"
      - "productType"
      - "priorityFlag"
      - "priorityReason"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Invalid API Usage"
    flow:
      - log: "Sending a request with an invalid 'addressId' query parameter."
      - get:
          url: "/searchTimeSlot?addressId=invalid&subscriberId={{ subscriberId }}&associatedDocument={{ associatedId }}&startDate={{ startDate }}&finishDate={{ finishDate }}&orderType={{ orderType }}&addressChangeFlag={{ addressChangeFlag }}&productType={{ productType }}&priorityFlag={{ priorityFlag }}&priorityReason={{ priorityReason }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_Token }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending a request with an unauthorized token."
      - get:
          url: "/searchTimeSlot?addressId={{ addressId }}&subscriberId={{ subscriberId }}&associatedDocument={{ associatedId }}&startDate={{ startDate }}&finishDate={{ finishDate }}&orderType={{ orderType }}&addressChangeFlag={{ addressChangeFlag }}&productType={{ productType }}&priorityFlag={{ priorityFlag }}&priorityReason={{ priorityReason }}"
          headers:
            Authorization: "Bearer wrongtoken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a request with a missing mandatory 'orderType' query parameter."
      - get:
          url: "/searchTimeSlot?addressId={{ addressId }}&subscriberId={{ subscriberId }}&associatedDocument={{ associatedId }}&startDate={{ startDate }}&finishDate={{ finish54Date }}&addressChangeFlag={{ addressChangeFlag }}&productType={{ productType }}&priorityFlag={{ priorityFlag }}&priorityReason={{ priorityReason }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_Token }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending a request to an invalid URL."
      - get:
          url: "/invalidURL?addressId={{ addressId }}&subscriberId={{ subscriberId }}&associatedDocument{{ associatedId }}&startDate={{ startDate }}&finishDate={{ finishDate }}&orderType={{ orderType }}&addressChangeFlag={{ addressChangeFlag }}&productType={{ productType }}&priorityFlag={{ priorityFlag }}&priorityReason={{ priorityReason }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_Token }}"
          expect:
            - statusCode: 404
      - think: 1
