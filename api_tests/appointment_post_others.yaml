# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointment_post for http method type POST in artillery framework
# RoostTestHash=b7eaf9bc25
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processenvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "appointment_post_others.csv"
    fields:
      - "slot_id"
      - "reason"
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      API_HOST: "https://staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Testing invalid slot ID in appointment creation"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processenvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "invalid-slot-id"
              reason: "{{ reason }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Testing missing authorization header"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
          json:
            appointment:
              slot:
                id: "{{ slot_id }}"
              reason: "{{ reason }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Testing excessive length for reason field"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processenvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "{{ slot_id }}"
              reason: "This is a very long reason that exceeds the maximum allowed length of 250 characters. This is a very long reason that exceeds the maximum allowed length of 250 characters. This is a very long reason that exceeds the maximum allowed length of 250 characters."
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Testing incorrect Content-Type header"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "text/plain"
            Authorization: "Bearer {{ $processenvironment.AUTH_TOKEN }}"
          body: "This should be a JSON object but is plain text instead"
          expect:
            - statusCode: 415
            - hasProperty: "error"
      - think: 1

      - log: "Testing invalid URL"
      - post:
          url: "/invalid-endpoint"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processenvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "{{ slot_id }}"
              reason: "{{ reason }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
