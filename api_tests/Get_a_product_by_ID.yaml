// Test generated by RoostGPT for test integrationtest-0408 using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

# configuration
target: https://example.com
phases:
  - duration: 60
    arrivalRate: 20

# scenarios
- name: Get a product by ID
  flow:
    - get:
        url: /products/1
        expect:
          - statusCode: 200
          - hasProperty: name
          - hasProperty: description
          - hasProperty: price

# assumptions:
#   - There is an existing product with ID 1

# expectations:
#   - The response status code should be 200
#   - The response should contain the product with ID 1

# additional comments added for clarity
# note: The payload is empty since we're sending a GET request
# note: The response should include the product with ID 1 in the body
