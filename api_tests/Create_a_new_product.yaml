// Test generated by RoostGPT for test integrationtest-0408 using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

# config
target: "$processEnvironment.SERVER_URL"
phases:
  - duration: 60
    arrivalRate: 20
variables:
  productId:
    - "2"
    - "3"
headers:
  Content-Type: "application/json"

# Scenario 1: Successful API request (DELETE /products/2)
scenario: Successful DELETE request
  - payload: null
  - headers: { 'Content-Type': 'application/json' }
  - capture:
      json: $.message
      as: responseMessage
  - assert:
      - statusCode: 200
      - responseMessage: "Product deleted"

# Scenario 2: API request with product not found (DELETE /products/invalid)
scenario: Invalid product ID
  - payload: null
  - headers: { 'Content-Type': 'application/json' }
  - capture:
      json: $.message
      as: responseMessage
  - assert:
      - statusCode: 404
      - responseMessage: "Product not found"

# Scenario 3: API request with invalid product id (DELETE /products/999999999999999999)
scenario: Large product ID
  - payload: null
  - headers: { 'Content-Type': 'application/json' }
  - capture:
      json: $.message
      as: responseMessage
  - assert:
      - statusCode: 404
      - responseMessage: "Product not found"

# Scenario 4: API request with valid product id (DELETE /products/3)
scenario: Valid DELETE request
  - payload: null
  - headers: { 'Content-Type': 'application/json' }
  - capture:
      json: $.message
      as: responseMessage
  - assert:
      - statusCode: 200
      - responseMessage: "Product deleted"
