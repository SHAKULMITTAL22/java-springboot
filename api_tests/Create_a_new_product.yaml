// Test generated by RoostGPT for test integrationtest-0408 using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

# configuration
target: "$processEnvironment.SERVER_URL"
phases:
  - duration: 60
    arrivalRate: 20

# scenarios
- name: Successful API request
  flow:
    - get:
        url: /products
        headers:
          Accept: application/json
        expect:
          - statusCode: 200
          - hasProperty: name

# assumptions
- there is a valid list of products

# additional comments added for clarity
# expectation: the GET request should return a list of products
# observation: the response should contain the name property
