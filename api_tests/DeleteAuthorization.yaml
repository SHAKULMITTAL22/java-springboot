# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# ROOST_METHOD_HASH=87771cdd6d
# ROOST_METHOD_SIG_HASH=0a68a12aee
# 
#    ########## Scenario ##########  
# 
# {
#   background: 'Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: 'Delete authorization\r\n' +
#     '    Given authorization ID\r\n' +
#     '    When the client sends a DELETE request to "/authorizations/ID"\r\n' +
#     '    Then the response status code should be 200 for a successful delete\r\n' +
#     '    And the authorizations should not appear in GET request for "/authorizations"',
#   title: 'Delete authorization'
# }
# 

# ********RoostGPT********
config:
  target: "http://localhost:8080"
  phases:
    - duration: 10 
      arrivalRate: 1
  variables:
    authorizationId: "123456789"

scenarios:
  - name: "Delete authorization"
    flow:
      - get:
          url: "/authorizations"
          capture:
            json: "$.*.id"
            as: "authorizationIds"
      - delete:
          url: "/authorizations/{{ authorizationId }}"
      - get:
          url: "/authorizations"
      - log: "Verifying authorization {{ authorizationId }} is deleted"
      - assert:
          - not_contains:
              json: "$.*.id"
              value: "{{ authorizationId }}"
          - statusCode: 200

  - name: "Verify authorization deleted"
    flow:  
      - get:
          url: "/authorizations/{{ authorizationId }}"
      - assert:
          - statusCode: 404
