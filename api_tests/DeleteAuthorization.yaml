# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# ROOST_METHOD_HASH=0df44641df
# ROOST_METHOD_SIG_HASH=038bdcdfb9
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Credential Wallet API\r\n' +
#     '  As a user of the Credential Wallet API\r\n' +
#     '  I want to be able to perform CRUD operations on accounts\r\n' +
#     '  So that I can manage my account effectively',
#   background: 'Background:\r\n    Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Delete authorization',
#     steps: 'Given authorization ID\r\n' +
#       'When the client sends a DELETE request to "/authorizations/ID"\r\n' +
#       'Then the response status code should be 200 for a successful delete\r\n' +
#       'And the authorizations should not appear in GET request for "/authorizations"',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "http://localhost:8080"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "DeleteAuthorization.csv"
    fields:
      - "authorizationID"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Delete authorization and validate"
    flow:
      - post:
          url: "/authorizations"
          json:
            email: "user@example.com"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.id"
            as: "authorizationID"
      - log: "Authorization created, proceeding with delete"
      - think: 1
      - delete:
          url: "/authorizations/{{ authorizationID }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
      - log: "Authorization deleted successfully"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - bodyJsonPath:
                expression: "$.!contains($.authorizations, {'id': '{{ authorizationID }}'})"
                ignoreError: false
      - log: "Validated that the authorization does not appear in GET request"
