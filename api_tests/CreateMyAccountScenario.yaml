# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4o
# 
# ROOST_METHOD_HASH=e8010dc6d7
# ROOST_METHOD_SIG_HASH=3a429a7e0a
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Credential Wallet API\r\n' +
#     '  As a user of the Credential Wallet API\r\n' +
#     '  I want to be able to perform CRUD operations on accounts\r\n' +
#     '  So that I can manage my account effectively',
#   background: 'Background:\r\n    Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Create my account',
#     steps: 'When the client sends a POST request "/accounts" with the accounts_body payload\r\n' +
#       'Then create an account with the specified informatio\r\n' +
#       'And verify the account created using GET request for "/me"',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/"
  plugins:
    expect: {}
  payload:
    path: CreateMyAccountScenario.csv
    fields:
      - email
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Create my account
    flow:
      - log: "Sending POST request to create an account"
      - post:
          url: "accounts"
          headers:
            Content-Type: application/json
          payload:
            email: "{{ email }}"
          expect:
            statusCode: 201
            headers:
              api-version: "1.0.0"
              commit-hash: "928d28d"
            json:
              - hasProperty: email
              - hasProperty: createdAt
              - hasProperty: updatedAt
      - think: 2
      - log: "Verifying the account created using GET request"
      - get:
          url: "me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            statusCode: 200
            headers:
              api-version: "1.0.0"
            json:
              - hasProperty: id
              - hasProperty: email
              - hasProperty: createdAt
              - hasProperty: updatedAt
              - hasProperty: name
              - hasProperty: image
              - hasProperty: settings
