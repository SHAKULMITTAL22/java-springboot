# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointment/{appointmentId}_delete for http method type DELETE in artillery framework
# RoostTestHash=8ae4a840f9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "appointment_appointmentId_delete_others.csv"
    fields:
      - "appointmentId"
      - "appointmentReason"
      - "appointmentComments"
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "https://staging.example.com"
    production:
      target: "https://example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Invalid API usage scenarios"
    flow:
      - log: "Sending request with invalid appointmentId"
      - delete:
          url: "/appointment/invalidAppointmentId?appointmentReason={{ appointmentReason }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Omitting required query parameter 'appointmentReason'"
      - delete:
          url: "/appointment/{{ appointmentId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Using an incorrect URL"
      - delete:
          url: "/invalidEndpoint/{{ appointmentId }}?appointmentReason={{ appointmentReason }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Sending request with excessive length in 'appointmentReason'"
      - delete:
          url: "/appointment/{{ appointmentId }}?appointmentReason=ThisIsAnExcessivelyLongReasonThatExceedsTheMaxLengthAllowedByTheAPI"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
