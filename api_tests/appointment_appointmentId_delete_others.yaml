# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_delete for http method type DELETE in artillery framework
# RoostTestHash=8ae4a840f9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_delete_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses for Delete Appointment"
    flow:
      - log: "Scenario: Testing invalid payloads for Delete Appointment API"
      
      - log: "Request: Missing required path parameter"
      - delete:
          url: "/appointment/"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 404
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
      
      - log: "Request: Invalid path parameter format"
      - delete:
          url: "/appointment/INVALID_ID"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
      
      - log: "Request: Missing required query parameter 'appointmentReason'"
      - delete:
          url: "/appointment/SA-562403"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
      
      - log: "Request: Invalid query parameter length for 'appointmentReason'"
      - delete:
          url: "/appointment/SA-562403?appointmentReason=" + "x" * 251
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
      
      - log: "Request: Invalid query parameter length for 'appointmentComments'"
      - delete:
          url: "/appointment/SA-562403?appointmentReason=Cancelamento%20Checkout&appointmentComments=" + "x" * 251
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
      
      - log: "Request: Unauthorized request with missing auth token"
      - delete:
          url: "/appointment/SA-562403?appointmentReason=Cancelamento%20Checkout"
      - expect:
          statusCode: 401
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
      
      - log: "Request: Non-existing appointment id"
      - delete:
          url: "/appointment/SA-999999?appointmentReason=Cancelamento%20Checkout"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 404
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
