# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-5-sonnet-20240620
# 
# ROOST_METHOD_HASH=e8010dc6d7
# ROOST_METHOD_SIG_HASH=3a429a7e0a
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Credential Wallet API\r\n' +
#     '  As a user of the Credential Wallet API\r\n' +
#     '  I want to be able to perform CRUD operations on accounts\r\n' +
#     '  So that I can manage my account effectively',
#   background: 'Background:\r\n    Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Create my account',
#     steps: 'When the client sends a POST request "/accounts" with the accounts_body payload\r\n' +
#       'Then create an account with the specified informatio\r\n' +
#       'And verify the account created using GET request for "/me"',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  payload:
    path: "{{ROOST_CSV_FILE_NAME}}"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Create and verify account
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            commit-hash: "928d28d"
          capture:
            - json: "$.token"
              as: "authToken"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - hasHeader: "commit-hash"

      - get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ authToken }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
