# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# ROOST_METHOD_HASH=dff35151d7
# ROOST_METHOD_SIG_HASH=3a7061432c
# 
#    ########## Scenario ##########  
# 
# {
#   background: 'Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: 'Create my account \r\n' +
#     '    When the client sends a POST request "/accounts" with the accounts_body payload\r\n' +
#     '    Then create an account with the specified informatio\r\n' +
#     '    And verify the account created using GET request for "/me"',
#   title: 'Create my account'
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  environments:
    myEnv:
      API_HOST: "http://localhost:8080"
      AUTH_TOKEN: ""
  payload:
    path: "CreateMyAccount.csv"
    fields:
      - "firstName"
      - "lastName" 
      - "email"
      - "password"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Create and verify account"
    flow:

      - log: "Send POST request to create account with payload data"  
      - post:
          url: "/accounts"
          headers:
            commit-hash: "928d28d"
          payload:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "firstName"
            - hasProperty: "lastName"  
            - hasProperty: "email"
            - headerEquals:
                api-version: "1.0.0"
            - headerEquals:  
                commit-hash: "928d28d"
      - think: 2

      - log: "Send GET request to /me to verify created account"
      - get:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200 
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - headerEquals:
                api-version: "1.0.0"
      - log: "Account created and verified successfully"
      - think: 2
