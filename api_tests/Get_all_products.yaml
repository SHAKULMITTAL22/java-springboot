// Test generated by RoostGPT for test integrationtest-0408 using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

# configuration
target: "$processEnvironment.SERVER_URL"
phases:
  - duration: 60
    arrivalRate: 20

# scenarios
- name: Get all products
  flow:
    - get:
        url: /api/products
        expect:
          - statusCode: 200
          - hasProperty: id
          - hasProperty: name
          - hasProperty: description
          - hasProperty: price

# assumptions
- there is no product with ID 1

# additional comments added for clarity
# expectation: the list of products returned should be empty
