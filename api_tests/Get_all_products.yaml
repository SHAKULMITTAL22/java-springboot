// Test generated by RoostGPT for test integrationtest-0408 using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

# Configuration
target: "{{ $processEnvironment.SERVER_URL }}"
phases:
  - duration: 300
    arrivalRate: 25

# Scenarios
scenarios:
  - name: Get all products
    flow:
      - get: /api/products
        headers:
          Authorization: Bearer <token>
        expect:
          statusCode: 200
          body: []
    comment: List of all products should be empty

  - name: Add a new product to the store
    flow:
      - post: /api/products
        headers:
          Authorization: Bearer <token>
        payload:
          name: Product 1
          description: This is the first product
        expect:
          statusCode: 201
          body:
            name: Product 1
            description: This is the first product
    comment: New product should be added successfully

  - name: Find product by ID
    flow:
      - get: /api/products/1
        headers:
          Authorization: Bearer <token>
        expect:
          statusCode: 200
          body:
            name: Product 1
            description: This is the first product
    comment: Product with ID 1 should be found

  - name: Update a product in the store with form data
    flow:
      - put: /api/products/1
        headers:
          Authorization: Bearer <token>
        payload:
          name: Updated Product Name
          description: This is the updated product description
        expect:
          statusCode: 200
          body:
            name: Updated Product Name
            description: This is the updated product description
    comment: Product with ID 1 should be updated successfully

  - name: Delete a product
    flow:
      - delete: /api/products/1
        headers:
          Authorization: Bearer <token>
        expect:
          statusCode: 204
    comment: Product with ID 1 should be deleted successfully
