# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointment_post for http method type POST in artillery framework
# RoostTestHash=b7eaf9bc25
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "appointment_post_success.csv"
    fields:
      - "slotId"
      - "reason"
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "https://api.staging.example.com"
      phases:
        - duration: 120
          arrivalRate: 10
  plugins:
    expect: {}

scenarios:
  - name: "Create appointment successfully"
    flow:
      - log: "Starting POST request to create an appointment"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "{{ slotId }}"
              reason: "{{ reason }}"
          expect:
            - statusCode: 201
            - hasProperty: "order"
            - hasProperty: "customer"
            - hasProperty: "appointment"
            - hasProperty: "address"
            - hasProperty: "products"
            - hasProperty: "control"
            - hasHeader: "Content-Type"
            - headerEquals: 
                Content-Type: "application/json"
      - think: 1
      - log: "Starting GET request to verify the appointment"
      - get:
          url: "/appointment/{{ slotId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "appointment.id"
            - matchesRegexp: 
                appointment.id: "^SA-\\d{6}$"
      - think: 1
