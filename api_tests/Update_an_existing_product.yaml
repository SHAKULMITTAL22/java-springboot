// Test generated by RoostGPT for test integrationtest-0408 using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

# Config
target: "$PROCESS_ENVIRONMENT_SERVER_URL"
phases:
  - duration: 60
    arrivalRate: 20
variables:
  productId:
    - "2"
    - "3"
headers:
  Content-Type: "application/json"

# Scenarios
scenario: Scenario 1: Successful Delete Request
  - delete:
        url: "/products/{{ productId }}"
        headers: "{{ headers }}"
        capture:
          json: "$.message"
          as: "responseMessage"
        think: 5
        assert:
          - statusCode: 200
            message: "Product deleted"
          - responseMessage: "Product deleted"

scenario: Scenario 2: API Request with Product Not Found
  - delete:
        url: "/products/invalid"
        headers: "{{ headers }}"
        capture:
          json: "$.message"
          as: "responseMessage"
        think: 5
        assert:
          - statusCode: 404
            message: "Product not found"
          - responseMessage: "Product not found"

scenario: Scenario 3: API Request with Invalid Product Id
  - delete:
        url: "/products/999999999999999999"
        headers: "{{ headers }}"
        capture:
          json: "$.message"
          as: "responseMessage"
        think: 5
        assert:
          - statusCode: 400
            message: "Invalid product id"
          - responseMessage: "Invalid product id"
