# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# ROOST_METHOD_HASH=803fe763f5
# ROOST_METHOD_SIG_HASH=c56a467b87
# 
#    ########## Scenario ##########  
# 
# {
#   background: 'Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: 'Grant authorization\r\n' +
#     '    Given an existing account, create another account\r\n' +
#     '    When the client sends a POST request to "/authorizations"\r\n' +
#     '    Then grant access to your account and confirm access via GET request to "/authorizations"',
#   title: 'Grant authorization'
# }
# 

# ********RoostGPT********
config:
  target: "http://localhost:8080"
  phases:
    - duration: 10 
      arrivalRate: 1
  payload:
    path: "accounts.csv"
    fields:
      - "email"
      - "password"  
      - "name"
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: "Grant authorization"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
            password: "{{ password }}"
            name: "{{ name }}"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "name"
          capture:
            json: "$.id"
            as: "accountId1"
      
      - post:  
          url: "/accounts"
          json:
            email: "{{ email }}"
            password: "{{ password }}"
            name: "{{ name }}"
          expect:
            - statusCode: 200
            - contentType: json  
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "name"
          capture:
            json: "$.id"
            as: "accountId2"
      
      - post:
          url: "/authorizations"
          json:
            accountId: "{{ accountId1 }}"
            grantAccessTo: "{{ accountId2 }}"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: "id"
            - hasProperty: "accountId"
            - hasProperty: "grantAccessTo"
          capture:  
            json: "$.id"
            as: "authorizationId"
      
      - get:
          url: "/authorizations"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: "authorizations"
            - hasProperty: "authorizations[*].id"
            - hasProperty: "authorizations[*].accountId"
            - hasProperty: "authorizations[*].grantAccessTo"
            - equals:
              - "authorizations[*].id"
              - "{{ authorizationId }}"
            - equals:  
              - "authorizations[*].accountId"
              - "{{ accountId1 }}"
            - equals:
              - "authorizations[*].grantAccessTo"  
              - "{{ accountId2 }}"
