# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4o
# 
# ROOST_METHOD_HASH=25259bbe63
# ROOST_METHOD_SIG_HASH=536a03cafe
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Credential Wallet API\r\n' +
#     '  As a user of the Credential Wallet API\r\n' +
#     '  I want to be able to perform CRUD operations on accounts\r\n' +
#     '  So that I can manage my account effectively',
#   background: 'Background:\r\n    Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Grant authorization',
#     steps: 'Given an existing account, create another account\r\n' +
#       'When the client sends a POST request to "/authorizations"\r\n' +
#       'Then grant access to your account and confirm access via GET request to "/authorizations"',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GrantAuthorization.csv"
    delimiter: "^|^"
    fields:
      - email
scenarios:
  - name: Grant authorization
    flow:
      - log: "Creating an account"
      - post:
          url: "/accounts"
          headers:
            Content-Type: application/json
          payload:
            email: "{{ email }}"
          expect:
            statusCode: 201
            headers:
              api-version: "1.0.0"
              commit-hash: "928d28d"
            json:
              hasProperty: token
      - think: 2
      - log: "Granting authorization to another account"
      - post:
          url: "/authorizations"
          headers:
            Content-Type: application/json
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "{{ email }}"
          expect:
            statusCode: 201
            headers:
              api-version: "1.0.0"
              location: "/authorizations/{{ email }}"
            json:
              hasProperty: token
      - think: 2
      - log: "Confirming authorization via GET request"
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            statusCode: 200
            headers:
              api-version: "1.0.0"
            json:
              hasProperty: grants
