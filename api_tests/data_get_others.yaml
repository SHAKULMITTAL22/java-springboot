# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /data_get for http method type GET in artillery framework
# RoostTestHash=b77323eb47
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "data_get_others.csv"
    fields:
      - "invalidUserId"
      - "invalidToken"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Invalid Authentication Token Test"
      - post:
          url: "/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            userId: "{{ invalidUserId }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Invalid URL Test"
      - get:
          url: "/invalidData"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1

      - log: "Invalid Payload Test"
      - post:
          url: "/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            userId: ""
          expect:
            - statusCode: 400
            - hasProperty: "validationError"
      - think: 1

      - log: "Missing Authentication Header Test"
      - post:
          url: "/data"
          json:
            userId: "{{ invalidUserId }}"
          expect:
            - statusCode: 403
            - hasProperty: "error"
      - think: 1
