# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=cff6ff5663
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "appointmentConfirmation_post_others.csv"
    fields:
      - "subscriberId"
      - "appointmentId"
      - "confirmation"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Testing invalid subscriber ID format"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "INVALID_SUBSCRIBER_ID"
            appointment:
              id: "{{ appointmentId }}"
              confirmation: "{{ confirmation }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing invalid appointment ID format"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            appointment:
              id: "INVALID_APPOINTMENT_ID"
              confirmation: "{{ confirmation }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing invalid confirmation status"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            appointment:
              id: "{{ appointmentId }}"
              confirmation: "maybe"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing unauthorized access"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            appointment:
              id: "{{ appointmentId }}"
              confirmation: "{{ confirmation }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing access with invalid token"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer INVALID_TOKEN"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            appointment:
              id: "{{ appointmentId }}"
              confirmation: "{{ confirmation }}"
          expect:
            - statusCode: 403
      - think: 1
