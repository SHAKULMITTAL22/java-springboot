# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=cff6ff5663
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointmentConfirmation_post_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests"
    flow:
      - log: "Testing invalid payload - missing required fields"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer: {}
            appointment: {}
      - expect:
          statusCode: 400
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing invalid payload - incorrect data types"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: 123456  # Should be a string
            appointment:
              id: 7891011  # Should be a string
              confirmation: "yes"  # Should be a boolean
      - expect:
          statusCode: 400
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing invalid authorization token"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalid_token"
          json:
            customer:
              subscriberId: "VTAL001"
            appointment:
              id: "SA-562403"
              confirmation: true
      - expect:
          statusCode: 401
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing invalid URL"
      - post:
          url: "/invalidEndpoint"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "VTAL001"
            appointment:
              id: "SA-562403"
              confirmation: true
      - expect:
          statusCode: 404
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing missing authorization header"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
          json:
            customer:
              subscriberId: "VTAL001"
            appointment:
              id: "SA-562403"
              confirmation: true
      - expect:
          statusCode: 401
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing invalid appointment ID"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "VTAL001"
            appointment:
              id: "INVALID_ID"
              confirmation: true
      - expect:
          statusCode: 400
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2
