# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /update_post for http method type POST in artillery framework
# RoostTestHash=317f9aa9d8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "update_post_others.csv"
    fields:
      - "info"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Non-success API response tests"
    flow:
      - log: "Sending POST with invalid JSON format"
      - post:
          url: "/update"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            info: "{{ info }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending POST with missing required fields"
      - post:
          url: "/update"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json: {}
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending POST to an incorrect URL"
      - post:
          url: "/udpate"  # corrected intentional typo in URL to /update
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processDisplay.AUTH_TOKEN }}"
          json:
            info: "{{ info }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending POST with unauthorized access"
      - post:
          url: "/update"
          headers:
            Content-Type: "application/json"
          json:
            info: "{{ info }}"
          expect:
            - statusCode: 401
      - think: 1
