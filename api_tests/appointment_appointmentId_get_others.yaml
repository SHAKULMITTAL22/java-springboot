# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointment/{appointmentId}_get for http method type GET in artillery framework
# RoostTestHash=b69bb60b36
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "appointment_appointmentId_get_others.csv"
    fields:
      - "appointmentId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses for Appointment Retrieval"
    flow:
      - log: "Testing with invalid appointmentId format"
      - get:
          url: "/appointment/invalidID"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 2
      - log: "Testing with missing appointmentId"
      - get:
          url: "/appointment/"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 2
      - log: "Testing with unauthorized access"
      - get:
          url: "/appointment/{{ appointmentId }}"
          headers:
            Authorization: "Bearer wrongToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 2
      - log: "Testing with numeric overflow in appointmentId"
      - get:
          url: "/appointment/999999999999999999999999999"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error"
      - think: 2
