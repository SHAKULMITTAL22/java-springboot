# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_get for http method type GET in artillery framework
# RoostTestHash=b69bb60b36
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Test Non-Successful API Responses
    flow:
      - log: "Testing invalid appointmentId format"
      - get:
          url: "/appointment/invalid-id"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 400
          hasHeader: "content-type"
          contentType: "application/json"
          hasProperties:
            - "error"
            - "message"
      - think: 2

      - log: "Testing missing authorization token"
      - get:
          url: "/appointment/123456"
      - expect:
          statusCode: 401
          hasHeader: "content-type"
          contentType: "application/json"
          hasProperties:
            - "error"
            - "message"
      - think: 2

      - log: "Testing non-existent appointmentId"
      - get:
          url: "/appointment/999999"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 404
          hasHeader: "content-type"
          contentType: "application/json"
          hasProperties:
            - "error"
            - "message"
      - think: 2

      - log: "Testing invalid URL"
      - get:
          url: "/invalid/endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 404
          hasHeader: "content-type"
          contentType: "application/json"
          hasProperties:
            - "error"
            - "message"
