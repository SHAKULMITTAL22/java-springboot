# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /appointment/{appointmentId}_patch for http method type PATCH in artillery framework
# RoostTestHash=33f5f2e325
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "appointment_appointmentId_patch_success.csv"
    fields:
      - "appointmentId"
      - "slotId"
      - "reason"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Patch Appointment Test Scenario"
    flow:
      - log: "Starting PATCH request to update an appointment"
      - patch:
          url: "/appointment/{{ appointmentId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "{{ slotId }}"
              reason: "{{ reason }}"
          expect:
            - statusCode: 200
      - think: 1
      - log: "Checking properties in the response"
      - expect:
          hasProperty: "order"
      - expect:
          hasProperty: "customer"
      - expect:
          hasProperty: "appointment"
      - expect:
          hasProperty: "address"
      - expect:
          hasProperty: "products"
      - expect:
          hasProperty: "control"
